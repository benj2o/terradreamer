# environments
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# macOS
.DS_Store

# data: cubes are large and reproducible from data/download_minicubes.py.
# Commit their checksums (data/raw.sha256), never the cubes themselves.
# Embeddings are reproducible from notebooks/phase1_2_encoders.ipynb Step 11.
data/raw/
data/cache/
data/greenearthnet/
data/embeddings/
data/masks/
data/phase*/
*.nc
*.nc.partial
*.zarr/

# build artifact: rebuild with ./make_zip.sh
phase1_*_repo.zip

# EO-WM author correspondence bundle: shared privately, unreleased core
# model code and a climatology artifact with its generation script
# withheld. See docs/correspondence/2026-08-07-eowm-authors.md.
vendor/eowm/

# Phase 1.2b: per-cube masks, cached once per cube (not per encoder)
data/masks/
