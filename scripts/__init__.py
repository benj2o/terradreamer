"""Project-tree tooling. Not part of the science; nothing here produces a number.

    inventory.py         list every file and folder, classify each movable unit
    organise_phases.py   file those units into the per-phase layout

The pair is deliberately split: the first only looks, the second only moves,
and the second imports its classification from the first so the plan you
approve in the listing is the plan that runs.
"""
