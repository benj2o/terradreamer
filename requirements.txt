# Phase 1.1 - data toy-load / Phase 1.2 - Tier A frozen encoders
earthnet
s3fs          # fetches pre-processed GreenEarthNet cubes (primary path)
earthnet-minicuber   # live-extraction path only, see data/download_minicubes.py
# imported by earthnet_minicuber/provider/s2/nbar.py but MISSING from that
# package's install_requires, so pip will not pull it in for you.
sen2nbar
torch
torchvision
satlaspretrain-models   # Phase 1.2: SatlasPretrain Sentinel-2 backbone
rasterio
xarray
zarr
netCDF4
dask
numpy
pandas
matplotlib
pytest
